/* Write a program to print several lines (such as your name and address). You
 * may use either several printf functions, each with a newline character in it,
 * or one printf with several newlines in the string. */
#include <stdio.h>

int main() {
  printf("Jacob Schroeder\n302 Flora Ave\n");

  return 0;
}
